# 🚀 Инструкция по деплою

## Vercel (Рекомендуется - самый простой способ)

### Шаг 1: Подготовка репозитория
1. Создайте репозиторий на GitHub
2. Загрузите код:
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin YOUR_REPO_URL
git push -u origin main
```

### Шаг 2: Деплой на Vercel
1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите "New Project"
3. Импортируйте ваш GitHub репозиторий
4. В настройках добавьте переменные окружения:
   - `TELEGRAM_BOT_TOKEN` = ваш токен от BotFather
   - `TELEGRAM_CHAT_ID` = ваш chat ID
5. Нажмите "Deploy"
6. Дождитесь завершения деплоя - получите рабочую ссылку!

### Шаг 3: Проверка
- Откройте ссылку, которую дал Vercel
- Сыграйте в игру и выиграйте
- Проверьте, что сообщение пришло в Telegram

## Другие платформы

### Netlify
1. Подключите GitHub репозиторий
2. Build command: `npm run build`
3. Publish directory: `.next`
4. Добавьте переменные окружения в настройках
5. Деплой!

### Railway
1. Создайте проект из GitHub репозитория
2. Railway автоматически определит Next.js
3. Добавьте переменные окружения
4. Деплой произойдёт автоматически

### Render
1. Создайте новый Web Service
2. Подключите GitHub репозиторий
3. Build Command: `npm install && npm run build`
4. Start Command: `npm start`
5. Добавьте переменные окружения
6. Деплой!

## ⚠️ Важные моменты

1. **Всегда добавляйте переменные окружения** на платформе деплоя
2. **Проверяйте работу Telegram бота** после деплоя
3. **Убедитесь, что вы написали боту** хотя бы одно сообщение (иначе он не сможет отправлять вам сообщения)

## 🔍 Проверка работы

После деплоя проверьте:
- ✅ Страница открывается
- ✅ Игра запускается
- ✅ Можно делать ходы
- ✅ При победе показывается промокод
- ✅ При победе приходит сообщение в Telegram
- ✅ При проигрыше приходит сообщение в Telegram

Успешного деплоя! 🎉


